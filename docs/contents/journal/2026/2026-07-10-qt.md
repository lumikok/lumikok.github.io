# 今日学习笔记

1. 工作流程，相关岗位信息，AI 冲击
2. 感受网页端 AI，与 agent 编程工具的差别
3. 讲了 git，GitHub，gitee
4. SVN 和 Git 区别,Git 操作流程，Git 特点（无网推送）
5. git 常用命令，报错解决方法，使用技巧（pull，分支）；版本冲突解决，分支合并（不污染主分支）

```bash
git branch <分支名>
git branch
git checkout <分支名>
git log --oneline # 显示commit信息
git reset --hard <id> # 强制回退
```

