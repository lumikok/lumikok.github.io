---
date: 2026-04-20
tags:
  - python
---
# 模块

目录：
- [介绍](#介绍);
- [导入模块](#导入模块);
- [自定义模块](#自定义模块);
- [包(package)](#包(package)).

## 介绍

- **模块(module)**：一个 `.py` 文件就是一个模块，模块是 `Python` 程序的基本组织单位，在模块中可以定义变量、函数、类，以及可执行的代码
-  **目的**：提高代码复用性，降低开发门槛，避免命名冲突

## 导入模块

**导入模块的具体语法如下**：

| 导入方式                      | 代码样例                               | 调用方式    | 调用样例                   |
| ------------------------- | ---------------------------------- | ------- | ---------------------- |
| import 模块名                | import random,os                   | 模块名.功能名 | random.randint(10,100) |
| import 模块名 as 别名          | import random as rd                | 别名.功能名  | rd.randint(10,100)     |
| from 模块名 import 功能名       | from random import randint,choice  | 功能名     | randint(10,100)        |
| from 模块名 import 功能名 as 别名 | from random import randint as rint | 别名      | rint(10,100)           |
| from 模块名 import *         | from random import *               | 功能名     | randint(10,100)        |

**注意**：以上语句，一般写在文件的开头

## 自定义模块

- **介绍**：开发复杂项目时，可以把项目拆分为若干个模块
- **注意**：每一个 `python` 文件都可以作为一个模块，**模块的名字就是文件的名字**（建议使用 `python` 标识符定义，规范命名）

- **特殊变量**：
	- **`__main__`**：非常重要的**内置变量**，表示的是当前模块的名称
		- 当模块直接运行时：`__name__` 的值为 "__name__"
		- 当模块被导入时： `__name__` 等于模块的文件名
	- **`__all__`**：模块级别的特殊变量，用于指定 **`from 模块名 import *`** 会导入哪些功能（`*`通配了哪些功能）

**示例代码如下**：
```python
# my_fun.py
# 常量（不会发生变化的数据，常量的名称为全部大写）  

PI = 3.1415926  
NAME = "Aremeno"  
  
# 函数  
def log_separator1():  
    print("- " * 30)  # "- ”重复输出30次  
  
def log_separator2():  
    print("+ " * 30)  
  
def log_separator3():  
    print("# " * 30)  
  
def log_separator4():  
    print("* " * 30)  
  
# 测试函数  
# __name__ ：Python中内置变量，表示的当前模块的名字（直接运行当前模块，__name__的值为"__name__" ；当该模块被导入时，__name__的值就是模块名字  
# 执行当前文件，则会执行如下代码；如果被当做模块导入，如下代码不执行  
if __name__ == '__main__':  
    log_separator1()
    
# main.py
# 导入模块  
# import my_fun  
  
# 使用模块中的功能  
  
# print(my_fun.PI)  
# print(my_fun.NAME)  
#  
# my_fun.log_separator1()  
# my_fun.log_separator2()  
  
# 导入自定义模块中的功能  
from my_fun import log_separator1,log_separator3,PI,NAME  
  
print(NAME)  
print(PI)  
  
log_separator1()  
log_separator3() # crtl + y 删除行
```

## 包(package)

- **介绍**：本质就是一个文件夹，该文件夹可以包含若干个 `python` 模块，文件夹下还包含了一个 `__init__.py` 文件
- **作用**：用于管理多个模块（包本质也是一个模块）
- **`__init__.py`**：描述当前包的信息，标识这是一个包，而不是普通的文件夹

**包的导入方式如下表所示**：


| 导入方式                   | 代码样例                                    | 调用方式       | 代码样例                          |
| ---------------------- | --------------------------------------- | ---------- | ----------------------------- |
| import 包名.模块名          | import utils.my_fun                     | 包名.模块名.功能名 | utils.my_fun.log_separator1() |
| from 包名 import 模块名     | from utils import my_fun                | 模块名.功能名    | my_fun.log_separator1()       |
| from 包名 import *       | from utils import *                     | 模块名.功能名    | my_fun.log_separator1()       |
| from 包名.模块名 import 功能名 | from utils.my_fun import log_separator1 | 功能名        | log_separator1()              |
| from 包名.模块名 import *   | from utils.my_fun import *              | 功能名        | log_separator1()              |

**注意**：在通过 `from 包名 import *` 导入全部模块的时候，需要在 `__init__.py` 文件中添加 `__all__=[]`，控制允许导入的模块列表
