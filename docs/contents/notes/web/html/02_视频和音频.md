# 视频和音频

## 视频标签

HTML5 引入了 `<video>` 标签，用于在网页中嵌入视频内容。基本语法如下：

```html
<video src="video.mp4" controls width="600" height="400">
  您的浏览器不支持视频标签。
</video>
```

该示例中：

- `src` 属性指定视频文件的路径。
- `controls` 属性添加视频控制界面（播放、暂停等）。
- `width` 和 `height` 属性设置视频显示的宽度和高度。
- 标签内的文本在浏览器不支持 `<video>` 标签时显示。
