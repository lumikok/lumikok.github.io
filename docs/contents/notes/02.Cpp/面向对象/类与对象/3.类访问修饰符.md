# 类的访问修饰符

关键词`public`、`protected`和`private`称为访问修饰符，用于控制类成员的访问权限。

一个类可以有多个访问修饰符区域，成员和类的默认访问修饰符是`private`。

## public

公有成员在程序中类的外部是可访问的，不需要任何特殊权限。

## protected

受保护成员在类的外部是不可访问的，但在派生类中是可访问的。

## private

私有成员在类的外部是不可访问的，甚至是不可查看的。只有类的成员函数和友元函数可以访问私有成员。
