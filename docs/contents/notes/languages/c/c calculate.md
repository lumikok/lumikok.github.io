# C语言运算符备忘录（随记）

## ？运算符的意义

条件运算符，也称为三元运算符，是C语言中唯一的三目运算符。  
它的语法格式如下：

```c
条件 ? 真值 : 假值
```

## sizeof 运算符

sizeof 运算符用于计算数据类型或变量所占的内存大小，单位是字节（byte）。

## 异或运算

异或运算符是“^”，它的作用是对两个二进制位进行比较，如果两个位相同则结果为0，不同则结果为1。
