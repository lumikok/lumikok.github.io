# Git

```bash
git add .

git commit -m "..."
git push -u origin main

```

```bash
git log
```

用于查看提交历史记录的核心命令，**按时间倒序**给出：commit id,author,date,comment

一般情况下，用 `git log --oneline` 只查看简短的 id 和备注（comment）即可。
