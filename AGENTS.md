# 笔记知识库协作规则

- 内容分线负责复习引导与具体知识正文，并在交接时给出准确文件清单；站点与发布总控负责导航、侧栏、链接、构建、Git 审计、提交、推送，以及周度或阶段性的结构治理。
- 写库任务串行交接。开始前先核对仓库根目录、分支、远端与工作区状态；不得静默覆盖其他任务或用户尚未提交的修改。
- 不擅自改写分线知识内容。发现事实、结构或学习证据问题时，退回对应任务协作确认。
- 发布时先运行侧栏生成、链接检查、构建与 `git diff --check`，再按交接清单精确暂存本批次文件、审查暂存区差异并执行 `git diff --cached --check`；无必要不修改站点配置。
- 一次提交只包含同一批次及其必要的生成文件或协议文件。提交信息冒号后的说明使用中文；推送后回报提交号、目标远端、检查结果和仍保留的未提交文件。
- 默认不追求笔记数量，不自动把教程改写成博客；仅在真实问题、亲自尝试或失败、可复现证据、非显然结论和迁移价值齐备时标记博客潜力。
