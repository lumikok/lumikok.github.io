# 笔记知识库协作规则

- 内容分线负责复习引导与具体知识正文，并在交接时给出准确文件清单；站点与发布总控负责导航、侧栏、链接、构建、Git 变更审计、准备精确的提交与推送命令，以及周度或阶段性的结构治理，Git 命令由用户自行执行并回报结果。
- 写库任务串行交接。开始前先核对仓库根目录、分支、远端与工作区状态；不得静默覆盖其他任务或用户尚未提交的修改。
- 不擅自改写分线知识内容。发现事实、结构或学习证据问题时，退回对应任务协作确认。
- 发布时先运行侧栏生成、链接检查、构建与 `git diff --check`，再按交接清单审计改动并向用户提供可直接执行的精确 `git add`、`git commit`、`git push` 命令；无必要不修改站点配置。
- 一次提交只包含同一批次及其必要的生成文件或协议文件。提交信息冒号后的说明使用中文；用户执行命令后回报提交号与推送结果，总控汇总目标远端、检查结果和仍保留的未提交文件。提交 `970ca96` 是用户明确授权总控代为提交并推送的本次例外，不改变后续规则。
- 默认不追求笔记数量，不自动把教程改写成博客；仅在真实问题、亲自尝试或失败、可复现证据、非显然结论和迁移价值齐备时标记博客潜力。
